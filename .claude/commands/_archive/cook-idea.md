---
description: Capture a raw idea into VLT_Content/HMN_A INPUTS. Usage: /idea <your idea text>
tags:
  - claude-config
---

Capture this idea using the Python tool. Run the following command:

```bash
python "c:/Workspace/Cook/AI_Tools/capture_idea.py" "$ARGUMENTS"
```

After the tool runs:
1. Confirm the file was written by showing the filename and path
2. Show the first 5 lines of the created file so the user can verify the content
3. Do not add commentary — just confirm and move on
